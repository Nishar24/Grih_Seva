# Backend - HomeEase

1. Copy `.env.example` to `.env` and fill values (MongoDB, JWT secret, Razorpay keys, SMTP).
2. Install dependencies:
   ```
   cd backend
   npm install
   npm run dev
   ```
3. Server runs on port defined in `.env` (default 5000).
