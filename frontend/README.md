# Frontend - HomeEase

1. Copy .env.example values into .env (create REACT_APP_API_URL=http://localhost:5000)
2. Install:
   ```
   cd frontend
   npm install
   npm start
   ```
3. Tailwind is included in devDependencies. For full Tailwind setup, run `npx tailwindcss init` if needed.
